[package]
name = "cardanowall-cli"
version = "0.12.0"
edition = "2021"
license = "Apache-2.0"
authors = ["CardanoWall <hello@cardanowall.com>"]
description = "The cardanowall CLI: a standalone Label 309 Proof-of-Existence verifier and toolkit."
repository = "https://github.com/cardanowall/label-309-cli"
homepage = "https://github.com/cardanowall/label-309-cli"
documentation = "https://docs.rs/cardanowall-cli"
readme = "README.md"
keywords = ["cardano", "label-309", "proof-of-existence", "cli", "verifier"]
categories = ["command-line-utilities", "cryptography"]

# The empty [workspace] table makes this crate its own workspace root, so cargo
# never walks upward looking for a parent workspace.
[workspace]

[[bin]]
name = "cardanowall"
path = "src/main.rs"

[dependencies]
# The Label 309 SDK crate `cardanowall` (from crates.io) — all record-prep,
# verification, sealed-PoE, merkle, and gateway-client logic lives there;
# this CLI is a thin argument-parsing + output shell over it. The pin is
# exact: this CLI release was built and tested against precisely this SDK
# version and nothing else.
cardanowall = "=0.12.0"
clap = { version = "4", features = ["derive", "env"] }
clap_complete = "4"
# Entropy for `identity --generate` (the same crate/major the SDK uses).
getrandom = "0.4"
glob = "0.3.3"
hex = "0.4.3"
owo-colors = "4"
rpassword = "7"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
sha2 = "0.11.0"
toml = "1"
zeroize = { version = "1", features = ["zeroize_derive"] }

[dev-dependencies]
tempfile = "3"
